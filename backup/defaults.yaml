# -*- coding: utf-8 -*-
# vim: ft=yaml
backup:
  archive_format: tar
  tar_options: -cvzf
  gzip_options: -v9

  encrypt_suffix: .sig

  dropbox:
    upload_url: https://content.dropboxapi.com/2/files/upload
